import { Text, StyleSheet, View } from "react-native";
import React, { Component } from "react";

export default class AssumptionItem extends Component {
  render() {
    return (
      <View>
        <Text>AssumptionItem</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
