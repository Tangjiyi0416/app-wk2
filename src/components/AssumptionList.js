import { Text, StyleSheet, View } from "react-native";
import React, { Component } from "react";

export default class AssumptionList extends Component {
  render() {
    return (
      <View>
        <Text>AssumptionList</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
